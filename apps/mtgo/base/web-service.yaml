apiVersion: v1
kind: Service
metadata:
  name: mtgo-web
spec:
  type: ClusterIP
  selector:
    app: mtgo-web
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
  externalIPs:
    - 192.168.1.101
